<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Unsplash API Demo</title>
</head>
<body>
    <h1>Unsplash API Demo</h1>
    
    <!-- Форма для поиска изображений по цвету -->
    <form action="/searchImagesByColor" method="post">
        <label for="color">Enter Color:</label>
        <input type="text" id="color" name="color" required>
        <button type="submit">Search Images by Color</button>
    </form>
    
    <!-- Результаты поиска изображений -->
    <div id="images">
        <% images.forEach(image => { %>
            <img src="<%= image.urls.regular %>" alt="<%= image.alt_description %>">
        <% }); %>
    </div>
</body>
</html>
